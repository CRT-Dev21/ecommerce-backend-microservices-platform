apiVersion: v1
kind: Service
metadata:
  name: inventory-db
  namespace: ecommerce
spec:
  selector:
    app: inventory-db-pod
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-db-deployment
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-db-pod
  template:
    metadata:
      labels:
        app: inventory-db-pod
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: "inventory_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgreuser"
          ports:
            - containerPort: 5432