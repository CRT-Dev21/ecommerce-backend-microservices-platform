apiVersion: v1
kind: Service
metadata:
  name: order-db
  namespace: ecommerce
spec:
  selector:
    app: order-db-pod
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db-deployment
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db-pod
  template:
    metadata:
      labels:
        app: order-db-pod
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: "order_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgreuser"
          ports:
            - containerPort: 5432